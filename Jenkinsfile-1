pipeline {
  agent { dockerfile true }
  stages {
    stage('Version Check') {
      steps {
        sh '''
          python3 --version
          pip --version
          ansible --version
          ansible-playbook --version
        '''
      }
    }
  }
}
